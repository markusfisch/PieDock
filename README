For copyright information see COPYING

Compile and install:

  $ ./configure
  $ make

  and then as root

  # make install

Configuration options for building:

  Use ./configure --help to see the full list of options

Run-time setup:

  Copy res/piedockrc.sample to ~/.piedockrc and modify it to meet your needs
